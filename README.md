# つづり（kakeibo-ledger）

日々の収入と支出を、気軽に記録・振り返るためのスマホ向け家計簿Webアプリです。

## できること

- 収入・支出を日付、カテゴリ、金額とともに記録
- 月ごとの収入・支出・残高を確認
- 支出カテゴリ別の内訳を表示
- 明細のカテゴリ絞り込み・削除
- データをブラウザの localStorage に保存

## 使い方

依存関係やビルドは不要です。リポジトリを取得し、`index.html` をブラウザで開いてください。

> データは利用中のブラウザだけに保存されます。サーバー同期、認証、銀行連携は未実装です。
